<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
  <session-factory>

    <!-- ============================= -->
    <!-- Database Connection Settings  -->
    <!-- ============================= -->
    <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
    <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/Students</property>
    <property name="hibernate.connection.username">Fatimah</property>
    <property name="hibernate.connection.password">jan2001</property>

    <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>
    <property name="hibernate.hbm2ddl.auto">update</property>
    <property name="hibernate.show_sql">true</property>
    <property name="hibernate.format_sql">true</property>

    <!-- ============================= -->
    <!-- ✅ L1 Cache                   -->
    <!-- Automatic - Always ON         -->
    <!-- ============================= -->
    <property name="hibernate.current_session_context_class">thread</property>

    <!-- ============================= -->
    <!-- ✅ L2 Cache Settings          -->
    <!-- ============================= -->
    <property name="hibernate.cache.use_second_level_cache">true</property>
    <property name="hibernate.cache.use_query_cache">true</property>
    <property name="hibernate.cache.region.factory_class">
        org.hibernate.cache.ehcache.EhCacheRegionFactory
    </property>
    <property name="net.sf.ehcache.configurationResourceName">
        /ehcache.xml
    </property>

    <!-- ============================= -->
    <!-- ✅ ONLY Student Mapping       -->
    <!-- ============================= -->
    <mapping class="com.example.model.Student"/>

    <!-- ❌ REMOVED — No tables exist for these -->
    <!-- <mapping class="com.example.model.LibraryCard"/> -->
    <!-- <mapping class="com.example.model.Address"/>     -->
    <!-- <mapping class="com.example.model.Course"/>      -->

  </session-factory>
</hibernate-configuration>